# Chrome 插件版

1. 打开 `chrome://extensions/`
2. 开启右上角“开发者模式”
3. 点击“加载已解压的扩展程序”
4. 选择克隆后的仓库根目录
5. 点击插件图标，监控会显示在 Chrome 右侧 Side Panel

插件页面直接连接：

```text
wss://api.dexscreener.com/token-profiles/latest/v1
```

只监控 Token Profile，默认勾选 Robinhood 和 ETH。已读事件保存在 Side Panel 的浏览器本地存储中。
